version: 3

systemUnderTest:
  service:
    definitions:
    - definition:
        source:
          filesystem:
            directory: .
        specs:
        - openapi.yaml

    runOptions:
      openapi:
        type: test
        baseUrl: http://localhost:3000/api/v1

specmatic:
  settings:
    test:
      schemaResiliencyTests: all

  license:
    path: ./license.txt
