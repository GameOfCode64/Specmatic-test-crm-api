version: 3

systemUnderTest:
  service:
    $ref: "#/components/services/teleCrmAuth"

components:
  services:
    teleCrmAuth:
      description: Tele CRM Auth Service
      definitions:
      - definition:
          source:
            filesystem:
              directory: .
          specs:
          - spec:
              id: teleCrmAuthSpec
              path: ./openapi.yaml
              actuatorUrl: http://localhost:3000/actuator/mappings

specmatic:
  # settings:
  # test:
  #   schemaResiliencyTests: 
  license:
    path: ./license.txt
